[target.wasm32-unknown-unknown]
rustflags = ["--cfg=getrandom_backend=\"wasm_js\""]

# NOTE:
# Linux 向けの mold 強制は削除しました（CI やローカルで RUSTFLAGS を使って任意に有効化してください）。
# 例:
#   RUSTFLAGS="-C link-arg=-fuse-ld=mold" cargo build
# もしくは .cargo/config.local.toml（VCS除外）でローカル限定設定にしてください。
